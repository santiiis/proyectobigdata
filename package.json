{
  "name": "plataforma-desercion",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @desercion/database generate",
    "db:push": "pnpm --filter @desercion/database push",
    "db:migrate": "pnpm --filter @desercion/database migrate",
    "db:seed": "pnpm --filter @desercion/database seed"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@9.15.0"
}
